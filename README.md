This is a java console that enables users to create and join study rooms, manage tasks, and communication through a simple chat system.

Compile
Open a terminal inside the project folder and run:
javac *.java

After compiling run:
java StudyRoomApp
